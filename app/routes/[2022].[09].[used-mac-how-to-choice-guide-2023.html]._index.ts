import { redirect } from "react-router";
import { url, status } from "../__generated__/[2022].[09].[used-mac-how-to-choice-guide-2023.html]._index";

export const loader = () => {
  throw redirect(url, status);
};
